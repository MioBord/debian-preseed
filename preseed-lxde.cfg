### 基础设置
d-i debian-installer/locale string zh_CN.UTF-8
d-i console-setup/ask_detect boolean false
d-i keyboard-configuration/xkb-keymap select cn

### 网络配置（DHCP 自动获取）
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string debian-lxde
d-i netcfg/get_domain string local

### 镜像源设置
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

### 分区设置（整盘自动分区，使用 ext4）
d-i partman-auto/method string regular
d-i partman-auto/choose_recipe select atomic
d-i partman-auto/confirm boolean true
d-i partman/confirm_write_new_label boolean true
d-i partman/confirm boolean true

### 用户设置
d-i passwd/root-password password rootpass123
d-i passwd/root-password-again password rootpass123
d-i passwd/make-user boolean false

### 时区设置
d-i time/zone string Asia/Shanghai
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean true

### 软件包选择（LXDE 桌面 + 常用工具）
tasksel tasksel/first multiselect lxde-desktop, ssh-server
d-i pkgsel/include string pcmanfm leafpad lxterminal lightdm

### 禁用推荐包以节省空间
d-i pkgsel/install-recommends boolean false

### 引导加载器安装
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean false

### 安装完成后自动重启
d-i finish-install/reboot_in_progress note
